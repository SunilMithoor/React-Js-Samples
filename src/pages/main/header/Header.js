import React from "react";

const Header = () => {
  return (
    <header
      style={{
        padding: "2px",
        background: "#343a40",
        color: "white",
        textAlign: "center",
      }}
    >
      <h2>Samples</h2>
    </header>
  );
};

export default Header;
